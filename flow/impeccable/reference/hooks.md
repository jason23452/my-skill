# Hooks

`hooks` 用於管理 design detector hook。hook 會在直接修改 UI 檔案後檢查常見設計問題，並回報提醒。

## Commands

- `hooks on`：啟用。
- `hooks off`：停用。
- `hooks status`：查看狀態。
- `hooks ignore-rule`：忽略特定規則。
- `hooks ignore-file`：忽略特定檔案。
- `hooks ignore-value`：忽略特定值。
- `hooks reset`：重置設定。

## 完成標準

依使用者要求更新 hook 設定，並回報目前狀態。

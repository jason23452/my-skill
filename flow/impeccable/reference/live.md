# Live

`live` 用於在 browser 中針對可見 UI 元素產生變體或做視覺迭代。此流程主要適用 web，不適用 native app code。

## 流程

1. 確認 dev server 與目標頁面。
2. 選取或定位要改的元素。
3. 產生 2 到 3 個視覺方向，不自動套用未確認方向。
4. 使用者選擇後再實作。
5. 驗證截圖、responsive 與互動狀態。

## 完成標準

迭代有明確 before/after，且不破壞現有功能。

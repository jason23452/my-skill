#!/usr/bin/env node

console.log("axios-token-baseurl-error: keep named wrappers in the owning feature/module, for example return api.get(endpoint, query, body, config). Nuxt projects use a $fetch-backed api.")

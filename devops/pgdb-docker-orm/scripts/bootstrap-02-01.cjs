#!/usr/bin/env node

const fs=require('fs');const file=['compose.yaml','compose.yml','docker-compose.yml','docker-compose.yaml'].find(f=>fs.existsSync(f))||'compose.yaml';let s=fs.existsSync(file)?fs.readFileSync(file,'utf8'):'';if(!s.trim())s='services:\n';const db='  db:\n    image: postgres:17-alpine\n    environment:\n      POSTGRES_USER: postgres\n      POSTGRES_PASSWORD: postgres\n      POSTGRES_DB: app_db\n    ports:\n      - \"5432:5432\"\n    volumes:\n      - postgres_data:/var/lib/postgresql/data\n    healthcheck:\n      test: [\"CMD-SHELL\", \"pg_isready -U postgres -d app_db\"]\n      interval: 5s\n      timeout: 5s\n      retries: 10\n';if(!/\n  db:|^  db:/m.test(s)){s=/^volumes:/m.test(s)?s.replace(/\nvolumes:/,'\n'+db+'\nvolumes:'):s.trimEnd()+'\n'+db}if(!/^volumes:/m.test(s))s=s.trimEnd()+'\n\nvolumes:\n  postgres_data:\n';else if(!/\n  postgres_data:|^  postgres_data:/m.test(s))s=s.trimEnd()+'\n  postgres_data:\n';fs.writeFileSync(file,s.endsWith('\n')?s:s+'\n')
